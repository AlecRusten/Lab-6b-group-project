public class Character {
    String Name;
    int Health;
    int RegenSpeed;

    public Character(String name, int health, int regenspeed)
        {
            Name = name;
            Health = health;
            RegenSpeed = regenspeed;
        }
}